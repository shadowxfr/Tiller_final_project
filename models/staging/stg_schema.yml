
version: 2

models:
    - name: stg_order_data
      description: "Order_data staging"
      columns:
        ### PK
          - name: id_order
            tests:
                - unique
                - not_null

    - name: stg_order_line_P
      description: "Nettoyage order_line"
      columns:
        ### PK
          - name: id_order_line
            tests :
              - unique
              - not_null                